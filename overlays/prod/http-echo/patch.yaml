apiVersion: apps/v1
kind: Deployment
metadata:
  name: http-echo
spec:
  replicas: 10
  template:
    spec:
      containers:
      - name: http-echo
        args:
          - "-text=Hello from PROD environment"
